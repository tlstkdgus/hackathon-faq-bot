# 주의: .gitignore는 줄 끝 주석(#)을 지원하지 않는다.
# 주석은 반드시 줄 맨 앞에서 시작해야 한다.

# 환경변수 / 토큰 — 절대 커밋 금지
.env
.env.local

# GitHub 개인 액세스 토큰 (Claude가 push할 때 읽는 파일)
# 이 파일이 커밋되면 토큰이 그대로 공개된다. 절대 금지.
.gh_token
*.token
.git-credentials

# 단, 값이 비어 있는 예시 파일은 커밋해야 한다.
# (예전엔 .gitignore에 .env.example이 그냥 들어 있어서,
#  예시 파일을 만들어도 git이 무시해 버리는 문제가 있었다.)
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# 못 찾은 질문 로그 (학생 질문 본문 포함 → 커밋 금지)
unanswered.log

# 사용 통계 로그 (사용자 ID 포함 → 커밋 금지)
stats.log

# 일일 리포트를 마지막으로 보낸 시각 (실행 중 생성되는 상태 파일)
digest_state.txt

# 노션 연동 시 로컬 폴백 캐시 (추후)
faq_cache.md

# 에디터 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
